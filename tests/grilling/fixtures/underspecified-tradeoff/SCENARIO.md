# Decision scenario

The project can either keep the current local cache or replace it with a
remote cache. Both options satisfy the functional requirement. The scenario
does not establish whether latency, operational simplicity, or portability is
the priority.
