interface:
  display_name: "Codex Session Recovery"
  short_description: "Find local Codex sessions and make safe recovery instructions."
  default_prompt: "Use $codex-session-recovery to find prior local sessions and provide CLI-first recovery steps."

policy:
  allow_implicit_invocation: false
