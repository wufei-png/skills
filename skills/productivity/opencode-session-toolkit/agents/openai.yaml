interface:
  display_name: "OpenCode Session Toolkit"
  short_description: "Inspect and export OpenCode sessions safely."
  default_prompt: "Use $opencode-session-toolkit to inspect or export local OpenCode sessions safely."

policy:
  allow_implicit_invocation: false
