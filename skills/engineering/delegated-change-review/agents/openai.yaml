interface:
  display_name: "Delegated Change Review"
  short_description: "Delegate review, verify findings, fix, test, and commit"
  default_prompt: "Use $delegated-change-review to review the current change with a read-only subagent, verify findings, fix as needed, test, summarize, and commit."

policy:
  allow_implicit_invocation: false
